name: Sanity Checks

on:
  workflow_call:
    inputs: {}
    secrets:
      repo_token:
        required: true

jobs:
  sanity:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"

      - name: Install dependencies
        uses: "./.github/workflows/workflow-install.yml"

      - name: Verify dependencies provenance
        run: npm audit signatures

      - name: Prettier
        uses: EPMatt/reviewdog-action-prettier@v1
        with:
          github_token: ${{ secrets.repo_token }}
          reporter: github-pr-review
          level: warning
          fail_on_error: true

      - name: Lint
        uses: reviewdog/action-eslint@v1
        with:
          github_token: ${{ secrets.repo_token }}
          reporter: github-pr-review
          level: warning
          fail_level: "error"

      - name: Typecheck
        uses: EPMatt/reviewdog-action-tsc@v1
        with:
          github_token: ${{ secrets.repo_token }}
          reporter: github-pr-review
          level: warning
          fail_on_error: true
